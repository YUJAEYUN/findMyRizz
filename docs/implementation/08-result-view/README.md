# Phase 8: Result View - 결과 조회

## 📋 개요

전화번호 인증 후 결과를 조회합니다.

---

## 🎯 목표

1. 전화번호 인증 (3회 제한)
2. 결과 조회 API
3. Presigned URL 생성
4. 개별 이미지 다운로드

---

## 📁 문서 구조

```
08-result-view/
├── README.md              # 이 파일
├── schemas.md             # Pydantic 스키마
├── services/
│   ├── phone-verification.md # 전화번호 인증
│   ├── result-fetcher.md     # 결과 조회
│   └── presigned-url.md      # Presigned URL
├── api.md                 # API 엔드포인트
└── tests.md              # 테스트
```

---

## 🔄 조회 흐름

```
1. GET /api/v1/results/{job_id}
   ↓
2. 전화번호 입력 요청
   ↓
3. POST /api/v1/results/{job_id}/verify
   - phone_number: "01012345678"
   ↓
4. 전화번호 일치 확인
   - 3회 실패 시 IP 차단 (1시간)
   ↓
5. JWT 토큰 발급
   ↓
6. 결과 조회
   - 원본 이미지
   - AI 생성 이미지 3장
   - 리포트
   ↓
7. Presigned URL 생성 (1시간)
   ↓
8. 개별 이미지 다운로드 제공
```

---

## 🔒 보안

### 전화번호 인증

- 최대 3회 시도
- 실패 시 IP 차단 (1시간)

### Presigned URL

- 1시간 유효
- 1회용

### JWT 토큰

- 1시간 유효
- job_id + phone_number

---

## 🔄 실행 순서

1. **스키마 정의** (`schemas.md`)
2. **전화번호 인증** (`services/phone-verification.md`)
3. **결과 조회** (`services/result-fetcher.md`)
4. **Presigned URL** (`services/presigned-url.md`)
5. **API 구현** (`api.md`)
6. **테스트** (`tests.md`)

---

## ✅ 완료 기준

- [ ] 전화번호 인증 동작
- [ ] IP 차단 동작
- [ ] JWT 발급
- [ ] 결과 조회 성공
- [ ] Presigned URL 생성
- [ ] 개별 이미지 다운로드

---

## 🚀 다음 단계

Result View 완료 → **Phase 9: Security**
