# Phase 1: Setup - 프로젝트 초기화

## 📋 개요

FastAPI 프로젝트를 처음부터 설정하는 단계입니다.

---

## 🎯 목표

1. FastAPI 프로젝트 생성
2. 환경변수 설정
3. 의존성 패키지 설치
4. 폴더 구조 생성

---

## 📁 문서 구조

```
01-setup/
├── README.md              # 이 파일
├── 01-project-init.md     # FastAPI 프로젝트 생성
├── 02-environment.md      # 환경변수 (.env)
├── 03-dependencies.md     # requirements.txt
└── 04-folder-structure.md # 폴더 구조
```

---

## 🔄 실행 순서

1. **프로젝트 초기화** (`01-project-init.md`)
   - Python 가상환경 생성
   - FastAPI 설치
   - 기본 main.py 생성

2. **환경변수 설정** (`02-environment.md`)
   - .env 파일 생성
   - 모든 API 키 및 설정값 정의

3. **의존성 설치** (`03-dependencies.md`)
   - requirements.txt 작성
   - pip install

4. **폴더 구조 생성** (`04-folder-structure.md`)
   - app/ 폴더 및 하위 구조 생성

---

## ✅ 완료 기준

- [ ] FastAPI 서버 실행 가능 (`uvicorn main:app --reload`)
- [ ] http://localhost:8000/docs 접속 가능
- [ ] 환경변수 로드 확인
- [ ] 모든 폴더 생성 완료

---

## 🚀 다음 단계

Setup 완료 후 → **Phase 2: Database**로 이동

